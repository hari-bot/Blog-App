<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit/Update Post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <div id="page">

        <%- include("partials/header.ejs") %>
        <div class="heading-section">
            <h1 class="heading p-3">Edit Your Posts</h1>
        </div>
    
        <div class="posts row mt-5">
            <% if (posts && posts.length > 0) { %>
                <% posts.forEach((post,index) => { %>
                    <div class="post  col-lg-4 col-md-6  col-12 mb-2" id=<%=index%> > 
                        <div class="card mx-2">
                            <div class="card-body">
                                <h5 class="card-title"><%= post.heading %></h4>
                                    <%- post.content?.replace(/<p>/g, '<p class="card-text">') %>
                                    <footer class="blockquote-footer">
                                        <small class="text-muted">
                                          <cite title="Source Title"><%= post.author %></cite>
                                        </small>
                                      </footer>
                               <div class="row">
                                <div class="col-6">
                                    <a class="btn btn-primary" href="/edit/<%=index%>">Edit</a>
                                </div>
                                <div class="col-6">
                                    <form action="/update/delete" method="post">
                                    <input type="hidden" name="deletePost" value=<%=index%>>
                                    <button class="btn btn-danger" type="submit">Delete</button>
                                </div>
                               </div>
                            </div>
                        </div>
                        </form>
                    </div>
                <% }); %>
            <% } else { %>
                <h4>No Posts Yet!<a href="/create"> Be the First to Share Your Thoughts.</a></h4>
            <% } %>    
        </div>
        <%- include("partials/footer.ejs") %>
    </div>

 
    
    
</body>
</html>